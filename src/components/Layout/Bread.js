import React from 'react'
import PropTypes from 'prop-types'
import pathToRegexp from 'path-to-regexp'
import styles from './Bread.less'

const Bread = ({ menu }) => {
  return (
    <div>this is bread!</div>
  )
}

Bread.propTypes = {
  menu: PropTypes.array
}

export default Bread
